# LumaBorderBox

```ts
import LumaBorderBox from '@luma/datav/border-box'
```

```vue
<LumaBorderBox :variant="8" :duration="3000">
  内容
</LumaBorderBox>
```

`variant` 1–13 对应 DataV borderBox1–13。亦可用 `:config="{ ... }"` 迁移旧配置。
