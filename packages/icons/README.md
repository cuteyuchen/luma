# @luma/icons

Luma 独立图标系统，提供响应式注册、分组、Iconify/组件/本地 SVG 适配、校验合成、data URI 缓存和带确认流程的图标选择器。

```ts
import { LumaIcon, LumaIconPickerDialog, registerIcons } from '@luma/icons'
import '@luma/icons/style.css'
```

详细说明见仓库文档 `docs/icons.md`。
